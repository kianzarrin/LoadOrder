- open directory
- auto-luncher tab
	- parameters as checkboxes (autosave to tool settings xml)
- tool settings tab [?]
- auto-publish using DefaultTargets https://stackoverflow.com/questions/12165715/call-publish-after-build-in-msbuild



* high Prioirty


active task: open profile features (mods/assets only | merge/replace):
	- package/plugin manager does not remove cached mod config data if mod is deleted.
		- this can be used to provide more info about missing items that is not stored in load order profile.
		- but maybe I should store extra data in profile.


